import { styleActionHeader } from "@/components/RowTemplate"

export const styleCardFile = `flex flex-column px-3 pt-0`;export const classeNameExcluir = styleActionHeader(`red`, `600`, `800`) + `
    pi
    pi-times
    border-circle
    top-0
    right-0
    p-1
    absolute`
export const styleExcluir = {
	color: `white`,
	margin: `-8px`,
	fontSize: `0.75rem`,
}
export const classesFilter = `flex flex-wrap gap-2`
export const classNameTable = `w-11 text-sm`
